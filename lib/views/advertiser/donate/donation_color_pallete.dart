import 'package:flutter/material.dart';

class AppColors {
  // Primary Color (Solid)
  static const Color primaryColor = Color.fromRGBO(255, 111, 97, 1.0);

  // Hint Color (15% Opacity)
  static const Color hintColor = Color.fromRGBO(255, 111, 97, 0.15);

  // Optional: Add other shades or related colors for theme consistency
  static const Color primaryDark = Color.fromRGBO(204, 89, 78, 1.0);
  static const Color primaryLight = Color.fromRGBO(255, 166, 158, 1.0);

  // Example neutral colors (can be customized)
  static const Color background = Color(0xFFFFFFFF);
  static const Color text = Color(0xFF333333);
  static const Color border = Color(0xFFE0E0E0);
}
